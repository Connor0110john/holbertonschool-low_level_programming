#include <stdio.h>

/**
 * main - check code
 *
 * Return: Always 0.
 */
#include <stdio.h>
int main(void)
{	void print_alphabet(void);
	char c;

	for (c = 'A'; c <= 'Z'; ++c)

	printf("%c ", c);
	return (0);
}
